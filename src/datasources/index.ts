const { BookAPI } = require('./bookAPI')
const { AnimalAPI } = require('./animalAPI')
const { SearchAPI } = require('./searchAPI')
const { CatFactsAPI } = require('./catFactAPI')

export default { BookAPI, AnimalAPI, SearchAPI, CatFactsAPI }